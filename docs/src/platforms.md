# Platform Support

Faker.cpp targets C++17 and uses only the standard library.

Tested in CI:

- Linux with GCC
- Windows with MSVC

macOS is expected to work because the implementation avoids platform-specific dependencies, but it is not tested in CI yet.

