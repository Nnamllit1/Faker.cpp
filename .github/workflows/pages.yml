name: Pages Deployment Disabled

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: ${{ false }}
    steps:
      - name: Deployment intentionally disabled
        run: echo "Pages deployment is disabled until the repository is made public."

